# Materials

Logical materials matching `.lmat` subset fields (`BaseColor`, `BaseColorMap`, `ShadingModel=DefaultLit|Unlit`). Defined in `Ps2CubeDemo.cpp` as `Ps2Material` PODs until EE `.lmat` load lands.

| Asset | Maps |
| --- | --- |
| `M_Cube` | `T_Checker_D` |
| `M_Ground` | `T_Grid_D` |
